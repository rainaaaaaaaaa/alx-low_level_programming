#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0
 * Return: Always (back)
 */

int main(void)

{
	int sd;

	for (sd = 0; sd < 10; sd++)
	putchar(sd);
	putchar('\n');
	return (0);
}
