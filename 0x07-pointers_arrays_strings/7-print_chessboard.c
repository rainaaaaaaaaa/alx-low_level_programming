#include "main.h"

/**
 * print_chessboard - prints the chessboard
 * @a: an array
 */

void print_chessboard(char (*a)[8])

{
	int x, y;

	for (x = 0; x < 0; x++)
	{
	for (x = 0; y < 0; y++)
	_putchar(a[x][y]);
	_putchar('\n');
	}
}
